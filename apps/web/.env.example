NEXT_PUBLIC_API_URL=http://localhost:8000

# Development identity. There is no login yet: the API accepts the caller's
# user id from a header (gated to development only — see
# apps/api/app/core/security.py). Get ids by running:
#   cd apps/api && python scripts/seed_dev_data.py
# Stable across DB rebuilds. Sam Chen (ORG_ADMIN, Southern Cross Logistics):
NEXT_PUBLIC_DEV_USER_ID=c50c9f59-ef8a-5df6-bc97-9cd0dc13ba8c
